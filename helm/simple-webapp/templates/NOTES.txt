The application has been deployed.

Frontend:
  - Service: {{ include "simple-webapp.fullname" . }}-frontend
  - Port: {{ .Values.frontend.service.port }}

Backend:
  - Service: {{ include "simple-webapp.fullname" . }}-backend
  - Port: {{ .Values.backend.service.port }}

To get the frontend URL (if using minikube):

  minikube service {{ include "simple-webapp.fullname" . }}-frontend -n {{ .Values.namespace.name }} --url

To check pods:

  kubectl get pods -n {{ .Values.namespace.name }}
